/**
 *  The different actions we can
 *  perform on an input
 */
enum RequestAction {
    Get = "get",
    Put = "put",
    Trigger = "trigger",
    Subscribe = "subscribe",
    Unsubscribe = "unsubscribe"
};

export default RequestAction;
